package api.crazy.demo.domain.usecases.<USECASEFOLDER>;

public class GetAll<ENTITY>Param {

    private GetAll<ENTITY>Param(){
    }

    //This is static becos not need instance... with put static you utilized without instance.
    //Only used a method
    public static GetAll<ENTITY>Param build(){
        return new GetAll<ENTITY>Param();
    }
    
}
